apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    url: timescaledb:5432
    user: postgres
    secureJsonData:
      password: password
    jsonData:
      database: postgres
      sslmode: disable
    uid: timescaledb
    isDefault: true
